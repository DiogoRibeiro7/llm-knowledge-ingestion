# Example Inputs

This directory contains MVP-compatible sample files for local ingestion runs.

Available samples:

- `sample.txt`
- `sample.md`
- `sample.json`

Run with:

```bash
python -m llm_knowledge_ingestion.cli.main --config configs/base.yaml
```
